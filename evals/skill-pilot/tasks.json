[
  {"id":"classifier","prompt":"Add a CDI AI service that classifies support tickets into BILLING, TECHNICAL or OTHER. Return an enum, configure a named small Ollama model called classifier with temperature zero, and expose POST /classify accepting a record with ticket text. Use declarative input guardrails for external text, delimit it as data in the prompt, and keep blocking calls off event-loop threads. Add declarative timeout and fallback. Do not call a live model. Ensure test-compile passes.","checks":["ai_service","enum","named_model","zero_temperature","input_guardrail","delimited_input","virtual_threads","fault_tolerance"]},
  {"id":"parallel","prompt":"Add a CDI multi-agent workflow that takes a product brief, runs a technical reviewer and a business reviewer in parallel, and combines their results. Use LangChain4j declarative agents and parallel composition with AgenticScope output, named Ollama models, record outputs, and input guardrails at the external input entry. Treat the brief as delimited data. Do not call a live model. Ensure test-compile passes.","checks":["ai_service","record","named_model","input_guardrail","delimited_input","parallel_agent","agent_annotation","agentic_scope"]},
  {"id":"rag","prompt":"Add a CDI document-question-answering service using Easy RAG with a local docs directory and an in-process embedding model. Include a sample document. Configure a named Ollama model, use a declarative AI service and input guardrails for the external question, delimit the question as data, and add declarative timeout and fallback. Do not call a live model. Ensure test-compile passes.","checks":["ai_service","named_model","input_guardrail","delimited_input","fault_tolerance","easy_rag_path","embedding_dependency","sample_document"]}
]
